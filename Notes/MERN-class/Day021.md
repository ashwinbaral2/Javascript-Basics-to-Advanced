# 📅 MERN CLASS — DAY 021

**Date:** Dec 25, 2025 - Thursday

**Topic:** JS Object Property Descriptors

---

## 🟠 1️⃣ THE THREE CORE FLAGS (MOST IMPORTANT)

**📌 OVERVIEW:**

Every object property in JavaScript has a hidden **"property descriptor"** behind it.

You usually see the **VALUE**, but JavaScript also tracks **HOW that value behaves**.

### writable

**Can the value be changed?**

```javascript
const obj1 = { a: 10 };

Object.defineProperty(obj1, "a", { writable: false });

obj1.a = 20;  // ❌ ignored (or error in strict mode)
```

### enumerable

**Does the property appear in loops?**

```javascript
const obj2 = {};

Object.defineProperty(obj2, "hidden", {
  value: 42,
  enumerable: false
});

Object.keys(obj2);  // [] → hidden is skipped
```

### configurable

**Can the property descriptor be changed or deleted?**

```javascript
const obj3 = {};

Object.defineProperty(obj3, "x", {
  value: 5,
  configurable: false
});

delete obj3.x;  // ❌ fails
// Object.defineProperty(obj3, "x", ...) ❌ fails
```

---

## 🟠 2️⃣ VALUE VS ACCESSOR PROPERTIES

**📌 VALUE PROPERTY:**

Uses `value` + `writable`

```javascript
const obj4 = { y: 10 };
```

**📌 ACCESSOR PROPERTY:**

Uses `get` / `set` (NO writable flag)

```javascript
const obj5 = {
  get z() {
    return this._z;
  },
  set z(val) {
    this._z = val;
  }
};

obj5.z = 100;
console.log(obj5.z);  // 100
```

---

## 🟠 3️⃣ IMPORTANT OBJECT METHODS

### Object.getOwnPropertyDescriptor()

Reveals the full descriptor:

```javascript
Object.getOwnPropertyDescriptor(obj1, "a");
// { value: 10, writable: false, enumerable: true, configurable: true }
```

### Object.defineProperty()

Create or modify **ONE** property:

```javascript
Object.defineProperty(obj1, "a", {
  writable: true
});
```

### Object.defineProperties()

Create or modify **MANY** properties at once:

```javascript
Object.defineProperties(obj1, {
  b: { value: 20, writable: true },
  c: { value: 30, writable: false }
});
```

---

## 🟠 4️⃣ STRUCTURE CONTROL (TOP-LEVEL)

### Object.preventExtensions()

**No new properties can be added**

```javascript
Object.preventExtensions(obj1);
obj1.newProp = 5;  // ❌ fails
```

### Object.seal()

**No add / delete allowed** (but `writable` still allowed)

```javascript
Object.seal(obj1);
obj1.a = 20;      // ✅ allowed (if writable)
delete obj1.a;    // ❌ fails
obj1.newProp = 5; // ❌ fails
```

### Object.freeze()

**No add / delete / update allowed** (completely locked)

```javascript
Object.freeze(obj1);
obj1.a = 20;      // ❌ fails
delete obj1.a;    // ❌ fails
obj1.newProp = 5; // ❌ fails
```

---

## 🟠 5️⃣ ONE-LINE MEMORY RULES

| Flag | Meaning |
|------|---------|
| `writable` | Can I **change** the value? |
| `enumerable` | Can I **see it in loops** (for...in, Object.keys)? |
| `configurable` | Can I **redefine or delete** it? |

**📌 KEY RELATIONSHIPS:**

```
freeze = seal + writable:false

seal ≠ freeze

Descriptors control BEHAVIOR, not just data
```

---

## 🟠 6️⃣ SUMMARY TABLE

| Method | Prevent Add | Prevent Delete | Prevent Modify |
|--------|-------------|-----------------|-----------------|
| `preventExtensions()` | ✅ | ❌ | ❌ |
| `seal()` | ✅ | ✅ | ❌ |
| `freeze()` | ✅ | ✅ | ✅ |