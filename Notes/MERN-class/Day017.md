# 📅 MERN CLASS — DAY 017

**Date:** Dec 18, 2025 - Thursday

---

## 🟠 1️⃣ OOP VS PROTOTYPE-BASED PROGRAMMING

**📌 KEY DIFFERENCE:**

Most languages are **Object-Oriented Programming (OOP)**:
- Use classes as blueprints
- Classical inheritance

**⚠️ JAVASCRIPT IS DIFFERENT:**

JavaScript is **prototype-based**:
- Objects inherit directly from other objects
- No traditional classes (ES6 classes are syntactic sugar)
- More flexible and dynamic

---

## 🟠 2️⃣ SYNCHRONOUS VS ASYNCHRONOUS

**📌 SYNCHRONOUS:**

- Code executes line by line
- Each statement waits for the previous one to complete
- Blocking behavior

```javascript
console.log("Start");
console.log("Middle");
console.log("End");
// Output: Start, Middle, End (in order, immediately)
```

**📌 ASYNCHRONOUS:**

- Code can execute without waiting
- Non-blocking behavior
- Allows other code to run while waiting

```javascript
console.log("Start");
setTimeout(() => console.log("Async"), 1000);
console.log("End");
// Output: Start, End, Async (after 1 second)
```

---

## 🟠 3️⃣ CLOSURES (IMPORTANT INTERVIEW QUESTION)

**📌 DEFINITION:**

A closure is a **nested function bundled with variables from its outer scope**.

The inner function has access to variables from the outer function, even after the outer function has returned.

**🔷 EXAMPLE:**

```javascript
function outer() {
  let name = "John"; // outer variable
  
  function inner() {
    console.log(name); // inner accesses outer variable
  }
  
  return inner;
}

const closure = outer();
closure(); // "John"
```

**📌 KEY POINT:**

The combination of the **function** + **variables it remembers** = **Closure**

---

## 🟠 4️⃣ CALL STACK VS EVENT LOOP

**📌 CALL STACK:**

- Tracks function calls
- Executes synchronous code
- LIFO (Last In, First Out)
- Blocks execution when full

**📌 EVENT LOOP:**

- Monitors the call stack and callback queue
- Moves callbacks from queue to stack when stack is empty
- Handles asynchronous operations
- Ensures non-blocking execution

**💡 SIMPLE EXPLANATION:**

Call Stack = **WHERE code executes**

Event Loop = **WHEN callbacks get to execute**

---

## 🟠 5️⃣ DATA STRUCTURES RECAP

**📌 QUEUE:**

- **FIFO** (First In, First Out)
- Methods: `enqueue()` and `dequeue()`
- Real-life: waiting line

**📌 STACK:**

- **LIFO** (Last In, First Out)
- Methods: `push()` and `pop()`
- Real-life: browser back button

---

## 🟠 6️⃣ VISUALIZING CODE EXECUTION

**📌 TOOL RECOMMENDATION:**

**JavaScript Visualizer 9000**

A tool to visually understand:
- Call stack execution
- Event loop behavior
- Callback queue processing
- Step-by-step code flow

**💡 USE IT TO:**

- Debug complex async code
- Understand execution order
- Learn how JavaScript actually works

---

## 🟠 7️⃣ IMPORTANT INTERVIEW JARGONS

**📌 KEY TERMS TO KNOW:**

- **async** → Declare asynchronous functions
- **await** → Wait for promises to resolve
- **promises** → Handle asynchronous operations
- **Global Execution Context (GEC)** → Where global code runs

---

## 🟠 8️⃣ STUDY TASKS

**📌 PRIORITY TOPICS:**

- ✅ Study **Stack** operations
- ✅ Study **Queue** operations
- ✅ Study **Event Loop** mechanism
- ✅ Understand their **interconnection**
