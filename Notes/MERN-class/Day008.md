# 📅 MERN CLASS — DAY 008

**Date: Dec 05, 2025 (Friday)**  
**Array Methods Continued**

---

## 🟠 1️⃣ LOOPS IN JAVASCRIPT

**📌 WHAT ARE LOOPS?**

Used to execute a block of code repeatedly until a condition is satisfied.

**🔷 TYPES OF LOOPS:**

- `for`
- `while`
- `do...while`
- `for...in`
- `for...of`

### For Loop

**📝 STRUCTURE:**

1. Declaration
2. Initialization
3. Condition
4. Iteration

**⚠️ NOTE:** Every developer must know the `for` loop, but it's tedious and error-prone. Not frequently used in real projects.

```javascript
for (let i = 0; i < 5; i++) {
  console.log(i);
}
```

### Preferred Modern Alternatives

**📌 MODERN LOOP METHODS:**

- `map()` → Transform data
- `forEach()` → Loop without returning
- `filter()` → Filter elements
- `reduce()` → Reduce to single value
- `find()` → Find first match
- `findIndex()` → Index of match
- `fill()` → Fill array with value

**⚡ PERFORMANCE NOTE:** These methods are slower than traditional `for` loops but offer better readability and maintainability.

```javascript
[1, 2, 3].map(n => n * 2);
[1, 2, 3].forEach(n => console.log(n));
[1, 2, 3].filter(n => n > 1);
```

---

## 🟠 2️⃣ VAR VS LET VS CONST (IMPORTANT)

**📌 COMPARISON TABLE:**

| Type | Scope | Redeclare | Reassign | Best Use |
|------|-------|-----------|----------|----------|
| `var` | Function/Global | ✓ Yes | ✓ Yes | Avoid |
| `let` | Block | ✗ No | ✓ Yes | When reassign needed |
| `const` | Block | ✗ No | ✗ No | Default choice |

**🎯 BEST PRACTICE:**

- Use `const` by default
- Use `let` when reassignment is required
- Avoid `var`

```javascript
var a = 10;
let b = 20;
const c = 30;
```

---

## 🟠 3️⃣ FOR...IN LOOP

**📌 WHAT IS IT?**

Used to iterate over object keys. **Not recommended for arrays.**

```javascript
const user = { name: "Ram", age: 21 };

for (let key in user) {
  console.log(key, user[key]);
}
```

---

## 🟠 4️⃣ FUNCTIONS (INTERVIEW FAVORITE)

**📌 DEFINITION:**

A reusable block of code that performs a specific task.

```javascript
function add(a, b) {
  return a + b;
}
```

### Parameters vs Arguments

**📝 DISTINCTION:**

- **Formal Parameters** → Variables in function definition
- **Actual Arguments** → Values passed during function call

```javascript
function greet(name) { // parameter
  console.log(name);
}

greet("Ram"); // argument
```

---

## 🟠 5️⃣ TYPES OF FUNCTIONS (INTERVIEW GOLD)

### Normal Function

```javascript
function sayHello() {
  console.log("Hello");
}
```

### Arrow Function

```javascript
const sayHelloArrow = () => {
  console.log("Hello");
};
```

**💡 KEY POINT:** Arrow functions do NOT bind their own `this`.

### Pure Function

```javascript
// Same input → Same output
// No side effects
function sum(x, y) {
  return x + y;
}
```

### Impure Function

```javascript
// Depends on external state
// Causes side effects
let counter = 0;
function increment() {
  counter++;
}
```

---

## 🟠 6️⃣ COMPILER & TRANSPILERS (CONCEPT)

**📌 COMPILER:**

Converts one language into another (e.g., English → Nepali)

**⚠️ NOTES:**

- Fun for experiments
- Not practical for large projects

**🔧 REAL PROJECTS USE:**

- Transpilers (Babel)
- JavaScript Engines (V8)

---

## 🟠 7️⃣ JAVASCRIPT INTERVIEW JARGON (MUST KNOW)

**📌 ESSENTIAL CONCEPTS:**

- Hoisting
- Scope (Global, Block, Function)
- Closure
- Callback
- Higher Order Function
- Event Loop
- Call Stack
- Lexical Scope
- Immutability
- Side Effects
- Promise
- Async / Await

---

## 🟠 8️⃣ REACT BASICS

**📌 WHAT IS REACT?**

- React is a **JavaScript LIBRARY** (not a framework)
- Used for building **User Interfaces (UI)**
- Commonly used for **Single Page Applications (SPA)**

---

## 🟠 9️⃣ LIBRARY VS FRAMEWORK

**📌 KEY DIFFERENCES:**

| Feature | Library (React) | Framework (Angular, Django) |
|---------|-----------------|------------------------------|
| Control Flow | Developer controls flow | Framework controls flow |
| Flexibility | High | Less flexible |
| Opinionated | No | Yes |
| Structure | You choose | Fixed structure |
| Learning Curve | Easier | Steeper |

**💡 QUICK RULE:**

- **Library** → You call the code
- **Framework** → Code calls you

---

## 🟠 🔟 JSX IN REACT

**📌 WHAT IS JSX?**

- Stands for **JavaScript XML**
- Allows writing **JavaScript inside HTML-like syntax**
- Makes UI code readable and expressive

```javascript
const element = <h1>Hello React</h1>;
```

---

## 🟠 1️⃣1️⃣ JAVASCRIPT DATA TYPES

**📌 PRIMITIVE TYPES:**

1. **String** → Sequence of characters (text)
2. **Number** → Integer and floating-point values
3. **Boolean** → `true` or `false`
4. **null** → Intentional empty value
5. **undefined** → Declared but not assigned
6. **Symbol** → Unique identifier
7. **BigInt** → Very large integers

**📌 NON-PRIMITIVE TYPE:**

8. **Object** → Arrays, objects, functions

**🔷 EXAMPLES:**

```javascript
let username = "John";                    // String
let score = 95;                           // Number
let isAdmin = false;                      // Boolean
let emptyValue = null;                    // null
let notAssigned;                          // undefined
let uniqueId = Symbol("id");              // Symbol
let bigNumber = 1234567890123456789n;    // BigInt
let data = {};                            // Object
```

---

## 🟠 1️⃣2️⃣ VARIABLES IN JAVASCRIPT

**📌 DEFINITION:**

Containers for storing data.

**🎯 BEST PRACTICES:**

- Use `let` when value can change
- Use `const` when value should not change

**📝 NAMING CONVENTION:**

Use **camelCase**

```javascript
let thisIsCamelCase;
let upperCase;
let freeCodeCamp;
```

**⚠️ IMPORTANT:**

Do NOT use reserved keywords as variable names (`let`, `const`, `function`, `return`, etc.)

---

## 🟠 1️⃣3️⃣ STRINGS IN JAVASCRIPT

**📌 WHAT ARE STRINGS?**

- Primitive data type
- Sequence of characters
- **Immutable** (cannot be changed after creation)

**💡 NOTE:** Arrays are **mutable** (changeable)

### String Concatenation

**📝 EXAMPLE 1:**

```javascript
let firstName = "John";
let lastName = "Doe";
let fullName = firstName + " " + lastName;

console.log(fullName); // John Doe
```

**📝 EXAMPLE 2:**

```javascript
let greeting = "Hello";
greeting += ", John!";

console.log(greeting); // Hello, John!
```
