# 📅 MERN CLASS NOTES — DAY 049

## **Date:** Feb 2, 2026 (Monday)

---

## 🔁 Redux Persist — Usage in a Project

**Redux Persist** is used to **save Redux state to persistent storage** (like `localStorage`) so data is not lost on page refresh.

### Why use Redux Persist?

* Keeps user logged in after refresh
* Preserves cart data in e‑commerce apps
* Improves user experience

### Typical Flow

1. Redux store updates
2. Redux Persist saves state to storage
3. On reload, state is rehydrated back into Redux

---

## 📄 SRS (Software Requirement Specification)

An **SRS** defines *what* a system should do — not *how*.

### Basic SRS Structure (Sample)

* **Introduction** (Purpose, Scope)
* **Overall Description**
* **Functional Requirements**
* **Non‑Functional Requirements**
* **System Architecture (High level)**
* **Constraints & Assumptions**

💡 *Good SRS = clarity for developers + confidence for stakeholders*

---

## 🧱 Express Boilerplate (GitHub)

An **Express boilerplate** is a pre‑structured backend setup that saves time.

Usually includes:

* `index.js` / `server.js`
* `routes/`
* `controllers/`
* `models/`
* `middlewares/`
* `.env`

🎯 Interview Tip: *Having your own clean boilerplate is a strong impression.*

---

## 🧠 Backend Classes — Core Concepts

### Node.js

* Built on **V8 Engine** (Chrome)
* **Runtime environment**, not a language
* Lightweight & fast
* Best for **I/O‑bound applications**

### Why Node.js?

* Non‑blocking I/O
* Event‑driven architecture
* High performance for concurrent requests

---

## 📁 File Handling in Node.js

Node.js provides the **`fs` module** (default feature).

Example:

```js
const fs = require('fs');

fs.readFile('file.txt', 'utf8', (err, data) => {
  if (err) throw err;
  console.log(data);
});
```

Used for:

* Reading/writing files
* Logs
* Upload handling

---

## 🧩 Microservice Architecture

Instead of one big backend:

* App is split into **small independent services**
* Each service does **one job well**

### Advantages

* Scalable
* Easier maintenance
* Tech‑stack flexibility

### Disadvantages

* Complex communication
* Requires DevOps maturity

---

## ⚡ Performance: Hits per Second (Conceptual)

| Technology | Strength                     |
| ---------- | ---------------------------- |
| Node.js    | Excellent for concurrent I/O |
| Python     | Slower for high concurrency  |
| Java       | Strong but heavier           |
| PHP        | Traditional request‑response |

➡️ Node.js shines in **I/O‑bound applications**, not CPU‑heavy ones.

---

## 🌐 Express Setup (Class Demo)

### Installation

```bash
npm install express --save
```

### Basic Server

```js
const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send('Hello World!');
});

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`);
});
```

### Notes

* Entry file: `index.js` (TypeScript later)
* Minimal but production‑ready base

---

## 🚀 Projects & Interview Insights

* Netflix clone / E‑commerce → **overused**
* Stand‑out projects should be:

  * Domain‑specific
  * Problem‑driven
  * Entrepreneurial

💡 *Think like a founder, not a tutorial follower.*

---

## 🔄 Nodemon

Used for **auto‑restarting server** on file changes.

```bash
npm install -g nodemon
```

Run:

```bash
nodemon index.js
```

---

## 📌 Tasks Before Next Class

* 📊 SQL vs NoSQL (comparison)
* 🧭 Install MongoDB Compass
* 🛠 Install MongoDB Community Server

---

## 🧬 What is ORM?

**ORM (Object Relational Mapping)** allows interaction with databases using objects instead of raw SQL.

### Example ORMs

* Sequelize (SQL)
* Prisma (SQL)
* Mongoose (MongoDB – ODM)

### Benefits

* Cleaner code
* Database abstraction
* Faster development

---

## 🧠 Key Takeaway

> *Node.js isn’t about speed alone — it’s about handling chaos efficiently.*

Learn fundamentals now. Frameworks come and go.
