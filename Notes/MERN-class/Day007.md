# 📅 MERN CLASS — DAY 007

**Date: Dec 04, 2025 (Thursday)**  
**Array Methods, Loops & Variable Declarations**

---

## 🟡 1️⃣ KEY CONCEPTS COVERED

**📌 TOPICS:**

- Preincrement and post-increment (`++a` vs `a++`) - also works for subtraction
- Array methods: `splice()`, `replace()`, `Math.round()`, `Math.floor()`, `.toFixed(2)`
- Mathematical operations: factorial, `Math.abs()`, `Math.pow()`, `Math.min()`, `Math.max()`
- Array basics and **Destructuring arrays**

---

## 🟡 2️⃣ EXAMPLE: FIND POSITIVE NUMBERS

**📌 FILTER POSITIVE NUMBERS FROM ARRAY:**

```javascript
const mixedNumbers = [-10, 15, -20, 25, 30, -5, 0];
const positiveNumbers = mixedNumbers.filter(num => num > 0);
console.log(positiveNumbers); // Output: [15, 25, 30]
```

---

## 🟡 3️⃣ EXAMPLE: GET HIGH SCORERS

**📌 FILTER & MAP COMBINED:**

```javascript
const students = [
    { name: "Alice", score: 85 },
    { name: "Bob", score: 78 },
    { name: "Charlie", score: 92 },
    { name: "David", score: 88 }
];

const highScorers = students
    .filter(student => student.score > 80)
    .map(student => student.name);
console.log(highScorers); // Output: ["Alice", "Charlie", "David"]
```

---

## 🟡 4️⃣ EXAMPLE: FILTER LONG WORDS

**📌 LENGTH FILTERING:**

```javascript
const wordList = ["cat", "house", "elephant", "dog", "umbrella", "sky"];
const longWords = wordList.filter(word => word.length > 4);
console.log(longWords); // Output: ["house", "elephant", "umbrella"]
```

---

## 🟡 5️⃣ PRACTICE CHALLENGE

**🎯 Q1. Select Random Months:**

Write a code that selects random months from an array of all months in a year and outputs them in a new array.

---

## 🟡 6️⃣ EXAMPLE: GET ADULT NAMES

**📌 FILTER BY AGE:**

```javascript
const people = [
    { name: "John", age: 17 },
    { name: "Jane", age: 22 },
    { name: "Jim", age: 19 },
    { name: "Jill", age: 15 }
];

const adults = people
    .filter(person => person.age >= 18)
    .map(person => person.name);
console.log(adults); // Output: ["Jane", "Jim"]
```

---

## 🟡 7️⃣ EXAMPLE: CALCULATE TOTAL WITH FOREACH

**📌 USING FOREACH LOOP:**

```javascript
const scores = [10, 20, 30, 40, 50];
let totalScore = 0;
scores.forEach(score => {
    totalScore += score;
});
console.log(totalScore); // Output: 150
```

---

## 🟡 8️⃣ EXAMPLE: PRINT NAMES IN UPPERCASE

**📌 FOREACH WITH STRING METHODS:**

```javascript
const names = ["Alice", "Bob", "Charlie", "David"];
names.forEach(name => console.log(name.toUpperCase()));
// Output:
// ALICE
// BOB
// CHARLIE
// DAVID
```


