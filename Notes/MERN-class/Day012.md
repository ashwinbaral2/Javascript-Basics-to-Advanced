# 📅 MERN CLASS — DAY 012

**Date:** Dec 11, 2025 - Thursday

**Topic:** JavaScript Objects

---

## 🟠 1️⃣ WHAT ARE OBJECTS?

**📌 DEFINITION:**

Objects store data in **key-value pairs**.

**🔷 EXAMPLE:**

```javascript
const person = {
  name: "Aadi",
  age: 20,
  hobby: "Chess"
};
```

---

## 🟠 2️⃣ ACCESSING VALUES

**📌 TWO WAYS TO ACCESS:**

### Dot Notation

```javascript
console.log(person.name); // "Aadi"
```

### Bracket Notation

```javascript
console.log(person["age"]); // 20
```

---

## 🟠 3️⃣ ADDING, UPDATING, DELETING

**📌 OPERATIONS:**

```javascript
person.country = "Nepal";   // Add new property
person.age = 21;            // Update existing property
delete person.hobby;        // Delete a property
```

---

## 🟠 4️⃣ FUNCTIONS INSIDE OBJECTS

**📌 DEFINITION:**

Functions stored in objects are called **methods**.

**🔷 EXAMPLE:**

```javascript
const user = {
  username: "coder123",
  greet() {
    return "Welcome, coder!";
  }
};

console.log(user.greet()); // "Welcome, coder!"
```

---

## 🟠 5️⃣ NESTED OBJECTS AND ARRAYS

**📌 KEY CONCEPT:**

Objects can contain arrays and other objects.

**🔷 EXAMPLE:**

```javascript
const student = {
  name: "Maya",
  skills: ["JS", "HTML", "CSS"],
  address: {
    city: "Kathmandu",
    ward: 5
  }
};

console.log(student.skills[0]);      // "JS"
console.log(student.address.city);   // "Kathmandu"
```

---

## 🟠 6️⃣ OBJECT METHODS FOR DATA HANDLING

**📌 KEY METHODS:**

```javascript
const car = {
  brand: "Toyota",
  model: "Corolla",
  year: 2020
};

Object.keys(car);
// ["brand", "model", "year"]

Object.values(car);
// ["Toyota", "Corolla", 2020]

Object.entries(car);
// [["brand", "Toyota"], ["model", "Corolla"], ["year", 2020]]
```

---

## 🟠 7️⃣ COPYING OBJECTS

**📌 SHALLOW COPY:**

```javascript
const copy1 = { ...car };
```

**📌 DEEP COPY:**

```javascript
const copy2 = structuredClone(car);
```

---

## 🟠 8️⃣ COMMON ARRAY OPERATIONS + OBJECTS

**📌 USEFUL ARRAY METHODS:**

```javascript
const numbers = [5, 2, 9, 1];

numbers.sort();           // [1, 2, 5, 9]
numbers.reverse();        // [9, 5, 2, 1]
numbers.map(n => n * 2);  // [10, 4, 18, 2]
numbers.filter(n => n > 3); // [5, 9]
```

---

## 🟠 9️⃣ USEFUL CONCEPTS

**📌 BEST PRACTICES:**

- ✅ Choosing the right data structure affects performance
- ✅ Prefer **objects** when accessing data by keys
- ✅ Prefer **arrays** for ordered lists
- ✅ Use **nested structures** to organize complex information

---

## 🟠 🔟 LINKEDIN 150-DAY CODING CHALLENGE

**📌 WHY POST DAILY?**

- Builds consistency and discipline
- Showcases your learning journey
- Helps build your developer brand

**📌 POST FORMAT RECOMMENDATION:**

- **Day number** (e.g., #Day012)
- **Topic learned** (e.g., JavaScript Objects)
- **Small code snippet** (clear, concise example)
- **Short reflection** or takeaway (what you learned)
