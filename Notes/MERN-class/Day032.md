## Date: 09 Jan 2026
# Day: Friday

# API & Components Notes 

## 📋 Restaurant Analogy

Customer (Client) → Waiter (API) → Cook (Backend Server) → Food (Frontend Data)


- **Customer**: Browser/app requesting data
- **Waiter (API)**: Communication bridge 
- **Cook**: Backend processes logic/data
- **Food**: Frontend serves UI/data

## 🔍 Browser DevTools - Network Tab
**F12 → 4th Tab (Network)** - Monitor all API calls:
- Request/response details
- Status codes (200✅, 404❌, 500💥)
- Timing & performance
- Headers, payloads

## 🌐 URL Anatomy
```
https://www.example.com:8080/users/123?name=john&age=25#profile
```
```
Protocol | Host:Port | Path | Query | Fragment
https:// | www.example.com:8080 | /users/123 | ?name=john&age=25 | #profile
```

## 🔗 Path vs Query Parameters

| Path Parameter | Query Parameter |
|----------------|-----------------|
| `/users/123`   | `/users?age=25&city=NYC` |
| **WHERE** (resource ID) | **HOW** (filters/options) |
| `amazon.com/cups/3` | `amazon.com/cups?color=blue` |

**"Go to cups section AND show blue ones only"**

## 🚀 HTTP Methods (CRUD)

| Method    | Action    | Use Case 
|--------   |--------   |----------
| **GET**   | Read      | Fetch users 
| **POST**  | Create    | Add new user 
| **PUT**   | Update (full) | Replace user 
| **PATCH** | Update (partial)| Change email only 
| **DELETE**| Delete    | Remove user 

## 🧪 Fake Store API - Test Immediately!
```
https://fakeapi.platzi.com/en/rest/
├── /users/          # GET all users
├── /users/{id}      # GET single user
├── /categories/     # GET categories
├── /products/       # GET products
└── /carts/          # GET carts
```

**Open in browser NOW!** 👆

## 🛠️ Project Setup

```bash
npm install axios
# OR use native Fetch API (no install)
```

### 📡 Basic Axios Example
```javascript
import axios from 'axios';

async function fetchUsers() {
  try {
    const { data: users } = await axios.get('https://fakeapi.platzi.com/en/rest/users/');
    console.table(users); // Pretty table in console
  } catch (error) {
    console.error('❌ API Error:', error.message);
  }
}

fetchUsers();
```

## ⚡ Promises vs Async/Await

```javascript
// Promise chaining (messy)
axios.get(url)
  .then(res => res.data)
  .then(data => console.log(data))
  .catch(err => console.error(err));

// Async/Await (clean!)
async function getData() {
  try {
    const { data } = await axios.get(url);
    console.log(data);
  } catch (error) {
    console.error('Error:', error);
  }
}
```

## 🎯 Next Steps Checklist
- [ ] Open Network tab (F12 → 4th tab)
- [ ] Visit `fakeapi.platzi.com/en/rest/users/`
- [ ] Install `npm install axios`
- [ ] Create first API call
- [ ] Monitor in Network tab

---

**💡 Pro Tip**: Always check Network tab first when debugging API issues!
```
