## 🗓️ Date:** 11 Jan 2026  
# Day : Sunday
---
# E-commerce Project Notes  

**Topic:** Data Fetching, Rendering, SEO

---
## API & Data Fetching

# Axios
- Axios is an API fetching tool
- Used to send HTTP requests (GET, POST, PUT, DELETE)
- Returns data in JSON format

# JSON
- JSON = JavaScript Object Notation
- Plain JavaScript object-like structure
- Used for data exchange between frontend and backend

---

## Fetch API Basics

```js
const res = await fetch('https://api.escuelajs.co/api/v1/products')
const data = await res.json()
```
# SEO (Search Engine Optimization)

* SEO helps websites rank better on search engines like Google

* Faster load + proper rendering = better SEO
# Rendering Types in React / Next.js

## 1. Client Side Rendering (CSR)

**Flow:**
Blank page → Fetch data → Show data in UI

markdown
Copy code

**Used for:**
- SEO-focused websites
- Public-facing sites
- Faster perceived loading (content loads dynamically)

## 2. Server Side Rendering (SSR)

**Flow:**
Fetch data → Create HTML with data → Show UI

markdown
Copy code

**Used for:**
- Internal tools
- Inventory systems
- Employee-only dashboards

class assignment code:
``` bash
'use client'
import EcomCard from '@/components/ecom-card'
import React, { useEffect, useState } from 'react'

// 1. blank page -----> data fetch ----> show in the blank UI 
    client side rendering 
    -used mostly for sites that need SEO in google, internet -- to load data faster
// 2. fetch data -----> create the html with the data -----> show in the UI
    sercer side rendering 
    -used when you wanna make inventory or direct site for employees..


//client side rendering
const Home =  () => {
  const [products, setProducts] = useState([])
  const fetchdata = async () => {
    const res = await fetch('https://api.escuelajs.co/api/v1/products')
    const data = await res.json()
    setProducts(data)
  }

  //this will run only once when the component is mounted
   useEffect(()=>{
    fetchdata()
   },[])
  return (
    <div>
      {JSON.stringify(products)}
    </div>
  )
}

export default Home
```