// Level 2 — Working with Multiple Promises

// Write two functions getUser() and getPosts(), each returning a promise that resolves after 2 seconds. Use async/await to fetch both and log them one after the other.

// Modify the previous question to fetch both simultaneously using Promise.all() and log the results.
